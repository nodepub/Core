{% extends '@np_admin/panel.twig' %}

{% block nav %}New {{ namespace }} Block{% endblock %}

{% block content %}
    <form action="{{ path('admin_post_blocks') }}" method="post" {{ form_enctype(form) }}>

        {{ form_errors(form) }}

        <div>
            {{ form_errors(form.content) }}
            {{ form_widget(form.content) }}
        </div>

        {{ form_rest(form) }}

        <a href="#" class="btn">Cancel</a>
        <input type="submit" class="btn btn-primary" value="Save" />
    </form>
{% endblock %}
<div id="block_{{ blockId }}" class="recent_posts">
    <h3>Recent Posts</h3>
    <ul>
        {% for post in posts %}
            <li><a href="{{ permalink(post) }}">{{ post.title }}</a></li>
        {% endfor %}
    </ul>
    <a href="{{ path('posts') }}" class="more">more posts</a>
</div>